// Ternary or Conditional
// (expression) ? true : false
// if expression is true , returns true part
// if expression is false, returns false part


let age = 18;
// let res:any = (age >= 18)?"Eligible to cast vote":"Not Eligible to cast vote";
let res:any = (age >= 18)?"Adult":"Minor";
console.log(res);


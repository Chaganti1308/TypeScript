// Check if a number is divisible by both 2 and 3.

let numberCheck = 8;

if(numberCheck%2 === 0 && numberCheck%3 === 0){
    console.log(`${numberCheck} is divisible by 2 & 3`);
    
} else {
    console.log(`${numberCheck} is not divisible  by 2 & 3`);
}
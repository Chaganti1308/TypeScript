// Compare two numbers and print the larger one.

let num1:number = 20;
let num2:number = 24;

if(num1 > num2){
    console.log(`${num1} is larger than than ${num2}`);
    
}else{
    console.log(`${num2} is larger than than ${num1}`);
}
